defmodule ColonyCore do
  @moduledoc """
  Shared primitives for the colony umbrella.

  The first milestone is not "more agent logic". It is a durable event envelope
  and a project shape that lets us prove replay, recovery, and coordination.
  """
end
